apiVersion: v1
kind: Service
metadata:
  name: equity-analytics-api
  labels:
    app: equity-analytics-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: equity-analytics-api
